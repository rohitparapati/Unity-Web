<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify One-Time Code</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="login-page">
    <div class="form-container">
        <img src="/images/logo.png" alt="Company Logo" class="logo">
        <h2>Verify One-Time Code</h2>
        <p>A 6-digit code has been sent to your email. Enter it below to complete your login.</p>
        <form action="/verify-otp" method="post">
            <input type="hidden" id="userId" name="userId">
            <div class="form-group">
                <label for="otp">One-Time Code:</label>
                <input type="text" id="otp" name="otp" placeholder="Enter 6-digit code" required autocomplete="off" maxlength="6">
            </div>
            <button type="submit" class="submit-btn">Verify</button>
        </form>
        <p>Didn't receive a code? <a href="/login">Try logging in again</a>.</p>
    </div>
    <script>
        // Set userId from localStorage
        document.getElementById('userId').value = localStorage.getItem('userId') || '';
        // Clear localStorage to prevent reuse
        localStorage.removeItem('userId');
    </script>
</body>
<footer>
    <div class="footer-content">
        <div class="left">
            © 2024 UnityWeb. All rights reserved.
        </div>                   
    </div>
</footer>
</html>